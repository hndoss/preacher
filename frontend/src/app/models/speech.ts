export class Speech {
    public id?: string;
    public title?: string;
    public preacher?: string;


    public constructor(id: string, title: string, preacher: string ) {
        this.id = id;
        this.title = title;
        this.preacher = this.preacher;

    }
}